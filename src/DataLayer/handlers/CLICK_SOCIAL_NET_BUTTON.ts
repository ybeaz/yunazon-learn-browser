import { store } from '../store'
import { ActionEventType } from '../../Interfaces/ActionEventType'

const { getState } = store

export const CLICK_SOCIAL_NET_BUTTON: ActionEventType = (event, data: any) => {}
