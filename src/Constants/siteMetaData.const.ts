import { SERVERS_MAIN } from '../Constants/servers.const'

export const SITE_META_DATA: Record<string, string> = {
  titleSite: 'YouRails - Teach curious; Learn from inspired',
  descriptionSite:
    'Behind every great human achievement there are teachers who helped to get on top. Courses, tests, certificates',
  canonicalUrlSite: `${SERVERS_MAIN.remote}`,
  langSite: 'en',
}
