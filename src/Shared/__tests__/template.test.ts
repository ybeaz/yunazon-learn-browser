import { template } from '../template'

describe('Test function template', () => {
  it('test', () => {
    const outputed = true
    const expected = true
    expect(outputed).toEqual(expected)
  })
})
