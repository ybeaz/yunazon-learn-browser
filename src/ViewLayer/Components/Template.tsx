import React, { useState, useEffect, useRef, ReactElement } from 'react'
import { useSelector, useDispatch } from 'react-redux'

export const Template: Function = (props: any): JSX.Element => {
  return <div className='Template'>null</div>
}
